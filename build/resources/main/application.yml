management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    git:
      enabled: true
    env:
      enabled: true
    build:
      enabled: true
  endpoint:
    shutdown:
      enabled: true        # shutdown ? ??? ???? ?
    health:
      show-components: always
  #      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"        #?? ??? ?????? ?

# info ?? ?????? ??? ????.
info:
  app:
    name: brunosong-actuator
